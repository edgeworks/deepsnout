# Copy to .env only to change defaults. No passwords here.
DEEPSNOUT_PORT=8080
DEEPSNOUT_ALLOWED_HOSTS=localhost,127.0.0.1,[::1]
# Set 1 behind a trusted HTTPS endpoint; do not expose the default HTTP port.
DEEPSNOUT_SECURE_COOKIES=0
